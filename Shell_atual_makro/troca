#! /bin/bash
#Escreva um algoritmo que armazene o valor 10 em uma variavel A e o valor 20 em uma variavel B. A seguir (utilizando apenas atribuicoes entre variaveis) troque os seus conteudos fazendo com que o valor que esta em A passe para B e vice-versa. Ao final, escrever os valores que ficaram armazenados nas variaveis.
clear
read -p "Informe um valor para A:" TIPO1
read -p "Informa um valor para B:" TIPO2
echo
echo "Valor informado para A = $TIPO1"
echo
echo "Valor informado para B = $TIPO2"
echo
echo "Valores invertidos para A = $TIPO2, para B = $TIPO1 "
echo
echo
